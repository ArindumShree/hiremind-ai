# AGENTS

## Memory workflow
- At the start of every session, read `memory.md` for project context.
- After completing a session's work, append a dated entry to `memory.md`
  (use the template already in the file). Keep it short: what was done,
  files changed, decisions/context, and open follow-ups.
- Keep entries concise; capture the "why" behind decisions.
